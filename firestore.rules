rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to authenticated users or public for web demo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
